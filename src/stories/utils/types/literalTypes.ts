import { calcRem } from 'utils/functions/calcFuntions';

export type size = 'small' | 'default' | 'large' | 'hulk';
export type buttonType = 'default' | 'outline' | 'inverted' | 'block';
export type colors = 'primary' | 'secondary' | 'mono';
export type horizontalMargin = 'left' | 'right' | 'only';
export type align = 'left' | 'right' | 'center';
export type direction = 'none' | 'left' | 'right' | 'up' | 'down';

// export const Size =

// none: 'none',
// tiny: `${calcRem(0)} ${calcRem(4)} ${calcRem(2)} ${calcRem(2)} rgba(0, 0, 0, 0.20)`,
// small: `${calcRem(0)} ${calcRem(6)} ${calcRem(3)} rgba(0, 0, 0, 0.20)`,
// default: `${calcRem(0)} ${calcRem(8)} ${calcRem(4)} rgba(0, 0, 0, 0.20)`,
// large: `${calcRem(0)} ${calcRem(12)} ${calcRem(8)} rgba(0, 0, 0, 0.20)`,
// hulk: `${calcRem(0)} ${calcRem(18)} ${calcRem(12)} rgba(0, 0, 0, 0.25)`,
